<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xu.home.mapper.blog.SysUserMapper">

    <resultMap id="BaseResultMap" type="com.xu.home.domain.blog.SysUser">
            <id property="id" column="id" jdbcType="INTEGER"/>
            <result property="account" column="account" jdbcType="VARCHAR"/>
            <result property="phone" column="phone" jdbcType="VARCHAR"/>
            <result property="password" column="password" jdbcType="VARCHAR"/>
            <result property="openid" column="openid" jdbcType="VARCHAR"/>
            <result property="unionid" column="unionid" jdbcType="VARCHAR"/>
            <result property="wxNickname" column="wx_nickname" jdbcType="VARCHAR"/>
            <result property="wxAvatar" column="wx_avatar" jdbcType="VARCHAR"/>
            <result property="isDelete" column="is_delete" jdbcType="TINYINT"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,account,phone,password,
        openid,unionid,wx_nickname,wx_avatar,
        is_delete,create_time,update_time
    </sql>

<!--    <insert id="insert" parameterType="com.xu.home.domain.blog.SysUser">-->
<!--        insert into sys_user (account,phone,password)-->
<!--        values-->
<!--        ()-->
<!--    </insert>-->

    <update id="deleteUser">
        update sys_user set is_delete = 1 where account = #{account}
    </update>

    <select id="selectUser" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sys_user
        where is_delete=0
        <if test="account != null and account != ''">
            and account=#{account}
        </if>
        <if test="password != null and password != ''">
            and password = #{password}
        </if>
        <if test="phone != null and phone != ''">
            and phone = #{phone}
        </if>
    </select>

    <select id="selectByOpenid" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sys_user
        where is_delete = 0 and openid = #{openid}
    </select>
</mapper>
